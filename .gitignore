# Dépendances et sorties de build
node_modules/
dist/
coverage/

# Divers
*.log
.DS_Store

# État local des agents : jamais versionné (données personnelles possibles) —
# bloc géré par agentsdir.
# agentsdir:begin ignore
/.agents/memory/
/.agents/output/
# agentsdir:end ignore

# npm pack output, produced by the tarball CI job and by local checks
*.tgz
